# ================================# UART_TX_UVM Simulation Run Script (run.do)# Run from the sim/ directory# ================================# Clean and recreate work directoryvdel -allvlib work# Compile files as one through filelistvlog -mfcu -cuname uart_tb_compile -sv -f filelist.f# Simulate with UVM controlvsim -uvmcontrol=all work.uart_tx_tb# Record sim logtranscript file sim_output.log# Plot waveformadd wave -r uart_tx_tb/*run -all#quit -sim